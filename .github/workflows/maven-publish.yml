name: Publish to Maven Central

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 1.2.3)'
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17 and Maven settings
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'
          server-id: Rhythmix
          server-username: ${{ secrets.CENTRAL_USERNAME }}
          server-password: ${{ secrets.CENTRAL_TOKEN }}

      # ✅ 自动触发用 tag，手动触发用输入参数
      - name: Extract version
        run: |
          if [ -n "${{ github.event.release.tag_name }}" ]; then
            TAG_NAME="${{ github.event.release.tag_name }}"
            RELEASE_VERSION="$(echo "${TAG_NAME}" | sed 's/^[vV]//')"
          else
            RELEASE_VERSION="${{ github.event.inputs.version }}"
          fi
          echo "RELEASE_VERSION=${RELEASE_VERSION}" >> $GITHUB_ENV

      # ✅ 使用 versions-maven-plugin 更新 pom.xml 版本
      - name: Set project version in pom.xml
        run: mvn versions:set -DnewVersion=${{ env.RELEASE_VERSION }}

      - name: Configure Git user
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      # ✅ 有变更即提交
      - name: Commit and push pom.xml changes
        run: |
          git add pom.xml
          git commit -m "chore(release): Set version to ${{ env.RELEASE_VERSION }}" || echo "No changes to commit"
          git push || echo "No changes to push"

      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}

      - name: Build and publish to Maven Central
        run: mvn -B -P release clean deploy central-publishing:publish
        env:
          CENTRAL_TOKEN: ${{ secrets.CENTRAL_TOKEN }}

      - name: Upload JAR to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name || env.RELEASE_VERSION }}
          files: ./target/rhythmix-${{ env.RELEASE_VERSION }}.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
